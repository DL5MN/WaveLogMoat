name: Build & Test

permissions:
  contents: read

on:
  push:
    branches: [main]
    tags-ignore:
      - 'v*'
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v6

      - name: Format
        run: |
          make format
          git diff --exit-code || { echo "::error::Code is not formatted. Run 'make format' locally."; exit 1; }

      - name: Lint
        run: make lint

      - name: Test
        run: make test

      - name: Build
        run: make build
